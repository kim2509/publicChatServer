<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tessoft.nearhere.news">

	<select id="getBigCities" resultType="java.util.HashMap">
		select r.*, sm.hostURL, sm.useYN from region r
			left outer join scraping_master sm on r.regionNo=sm.regionNo
		where parentNo is null
		and isHotSpot is null
		order by priority 
	</select>
	
	<select id="getRegionByParent" parameterType="String" resultType="java.util.HashMap">
		select r.*, sm.* from region r
			left outer join scraping_master sm on r.regionNo=sm.regionNo
		where parentNo = #{value}
		and useYN='Y'
		order by r.priority
	</select>
	
	<insert id="insertFavoriteRegion" parameterType="java.util.HashMap">
        insert into user_favorite_region(userID, regionNo, createdDate)
        values
	        <foreach collection="list" item="item" separator=" , ">
	            (#{item.userID}, #{item.regionNo}, NOW() )
	        </foreach>
	 </insert>
 
 	<select id="getFavoriteRegionByUser" parameterType="String" resultType="java.util.HashMap">
		select r.* from user_favorite_region ur
			left outer join region r on ur.regionNo=r.regionNo
		where ur.userID=#{value}
		order by priority
	</select>
	
	<delete id="deleteRegionByUser" parameterType="String">
		delete from user_favorite_region
		where userID=#{value}
	</delete>
	
	<insert id="insertNews" parameterType="java.util.HashMap">
        insert into news(regionNo, title, link, createdDate )
        values
	        <foreach collection="list" item="item" separator=" , ">
	            (#{item.regionNo}, #{item.title}, #{item.link}, NOW() )
	        </foreach>
	 </insert>
	 
	<delete id="deleteNewsByRegion" parameterType="String">
		delete from news
		where regionNo = #{value}
	</delete>
	
	<select id="getNewsByRegion" parameterType="String" resultType="java.util.HashMap">
		select * from news
		where regionNo = #{value}
	</select>
</mapper>